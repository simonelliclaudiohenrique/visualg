Algoritmo "fatorial"

Var

   N, i, fatorial: inteiro

Inicio

   escreva ("Digite o valor de N:")
   leia (N)

   fatorial <- 1
   para i de 1 ate N faca
      fatorial <- fatorial * i
   fimpara

   escreval ("FATORIAL =", fatorial)

Fimalgoritmo